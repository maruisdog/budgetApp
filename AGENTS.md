# budgetApp AGENTS

## 프로젝트 설명
- 이 저장소는 CSV 기반 가계부 CLI 앱 프로젝트다.
- 거래 데이터는 `data/*.csv` 파일 형식을 기준으로 다룬다.

## 코딩 규칙
- 모든 함수와 주요 데이터 구조에는 타입 힌트를 반드시 작성한다.
- 함수 하나의 길이는 최대 50줄을 넘지 않도록 유지한다.

## TDD 규칙
- 구현 전에 반드시 테스트를 먼저 작성한다.
- 새 기능은 실패하는 테스트를 확인한 뒤 구현으로 진행한다.

## 품질 규칙
- 함수의 순환복잡도는 10 이하로 유지한다.

## 품질 검수 규칙
- 커밋 전에 반드시 `qa_engineer` 서브에이전트로 품질 검수를 수행한다.
- 검수 대상은 테스트 통과 여부, 타입 힌트, 순환복잡도, 엣지 케이스다.

## 테스트 실행 명령어
- `pytest`
- `radon cc`

## 커밋 규칙
- 하나의 기능이 개발되면 커밋하고 푸시한다.
- 커밋은 작고 명확하게 유지한다.
